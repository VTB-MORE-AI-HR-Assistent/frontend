# Production Environment Variables

# Application URL (update with your actual production URL)
NEXT_PUBLIC_APP_URL=https://vtbaihr.com

# API Configuration 
# For client-side requests, use the server's public address with the gateway port
# Update this with your actual server IP or domain
NEXT_PUBLIC_API_URL=http://localhost:8081
NEXT_PUBLIC_API_TIMEOUT=30000

# For server-side requests (SSR), we can use the Docker network
# This is used in API routes and server components
API_URL_INTERNAL=http://api-gateway:8081

# Environment
NEXT_PUBLIC_ENV=production

# Daily.co Configuration (production keys)
NEXT_PUBLIC_DAILY_DOMAIN=https://hraiassistant.daily.co
NEXT_PUBLIC_DAILY_API_KEY=3c007e94708cb73db9f811468463dfd994f1f68d851abaa5d6bb137a9e3b2e76